# 2026-09-23 工作日志

## 提取音乐模块为独立页面 `music.html`
- 需求：把主页 `index.html` 的音乐部分提取为单独 HTML 页面，且不改动任何原有文件。
- 产物：新增 `music.html`（项目根目录，703 行，单文件，内联 JS）。
  - 复用原有 `./static/css/style.css` + `./static/css/root.css`，视觉与主页完全一致（含明暗主题变量）。
  - 页面内 `<style>` 仅做独立页布局覆盖：`body.music-page` 居中、`.music-card` 由 absolute 改为 static。
  - 内联脚本 = 原 `static/js/script.js` 的 `musicPlaylistConfig`(第10-16行) + 音乐逻辑(第418-937行)，原样提取未改。
- 原文件校验：`index.html` / `script.js` / `style.css` / `root.css` MD5 前后一致，未改动。
- 验证：本地 8788 端口静态服务 200；歌单接口 `api.qijieya.cn/meting` 返回正常；内联 JS 通过 `node --check`。
- 关键结构定位（便于下次维护）：
  - HTML 音乐卡片：`index.html` 第196-279行
  - JS 配置：`static/js/script.js` 第10-16行
  - JS 音乐逻辑：`static/js/script.js` 第418-937行（以 `initializeMusicPlayer();` 结尾）
  - CSS：`style.css` 第461-937行（`.music-card`/`.mw-*`）+ 主题变量第91-96/125-130行 + 媒体查询第1803-1812、1939-1944行

## 追加：music.html 改为完全自包含（CSS 也内联）
- 需求：把音乐相关的 CSS 与 JS 全部复制进这一个 HTML 文件。
- 现状：`music.html` 1379 行、约 41KB，**无任何外部本地 css/js 引用**（JS 原本已内联）。
- 内联的 CSS 来源切片（verbatim，未改写）：
  - `style.css` 1-61（@font-face ×3 + 全局 reset + 滚动条）、65-131（Light/Dark 主题变量）、133-147（body 背景）、312-321（`.zyyo-filter`）、461-937（音乐卡片全部样式）、1939-1944（≤480px 隐藏音量条）
  - `root.css` 132-150（最后一个 `html{}` 主题，提供 `--main_bg_color` 壁纸）
- 内联后必须做的路径修正（关键坑）：
  - `url(../fonts/` → `url(./static/fonts/`
  - `url(../img/` → `url(./static/img/`（root.css 的 `--main_bg_color` 背景图）
  - 原因：原 css 在 `static/css/` 下用 `../`，内联到根目录后相对基准变了。
- 仍需外部引用的（非音乐样式，故意保留）：Font Awesome CDN 图标字体；本地 `./static/fonts/*` 与 `./static/img/background.jpg` 作为资源（未 base64 内联，避免体积膨胀）。
- 校验：CSS 花括号平衡（86/86）、JS 通过 `node --check`、4 个原文件 MD5 仍与改动前一致。
- 复用脚本：`C:/Users/27563/AppData/Local/Temp/gen_music_page2.py`（按行号切片生成，可重复执行）。

## 追加 v3：music.html 改为「拷走即用」单文件（最终版）
- 需求：只要音乐卡片相关内容，去掉背景图等一切本地资源依赖，随便拷到哪里都能直接打开。
- 最终产物：`music.html` **1323 行 / 43KB**，全文 **零外部本地引用**（`src`/`href`/`url()` 只剩 data: 与 https:）。
- 相比 v2 的改动：
  1. 删除 `@font-face` 与字体文件引用 → 改用系统字体栈 `--mw-font` / `--mw-mono`；原 CSS 里的 `"a", sans-serif` / `"a", monospace` 已替换为变量。
  2. 删除 `root.css` 主题块与 `--main_bg_color` 壁纸 → body 改为自绘径向渐变（Light `#fdfefe→#dde7f3`；Dark `#1b2333→#0c1119`）。
  3. 删除 `.zyyo-filter` 遮罩层、favicon.gif 外链（改为 data-uri SVG 图标）。
  4. **去掉 Font Awesome CDN**：11 个图标全部改为 CSS mask + 内联 data-uri SVG，`<i>` 元素与 JS 依赖的 `mw-icon-*` 类名、`hidden` 属性全部保留（关键：`hidden` 只有 HTMLElement 有，若换成 `<svg>` 会让 JS 的 `el.hidden = true` 失效，故必须保留 `<i>` 标签）。
  5. 保留 `.music-widget.mode-shuffle .mw-icon-shuffle` 的显隐逻辑，但 `display: inline-flex !important` → `inline-block !important`（非字体图标下 inline-flex 会塌陷）。
  6. `.mw-cover-placeholder` 由 `display:flex` 居中改为 `inset:0 + margin:auto` 绝对居中（SVG mask 不能当 flex 容器）。
- 校验：CSS 括号 93/93 平衡；11 个 data-uri SVG 全部 XML 可解析；内联 JS 通过 `node --check`；4 个原文件 MD5 全程未变。
- 生成脚本：`C:/Users/27563/AppData/Local/Temp/gen_music_page3.py`。
- 备注：无头浏览器截图验证在本机跑不起来（agent-browser 冷启动 >2min 被终止），改为静态校验 + 人工预览。
